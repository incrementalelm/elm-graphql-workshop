# import CurrentWeather from "current-weather.graphql"
# import FiveDayForecast from "five-day-forecast.graphql"
# import Language, Units from "shared.graphql"

type Query {
  currentWeather(lang: Language = en, units: Units = standard): CurrentWeather
  fiveDayForecast(lang: Language = en, units: Units = standard): FiveDayForecast
}
